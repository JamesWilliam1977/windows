interface:
  display_name: "Windows"
  short_description: "Repo-specific patterns and workflows for windows"
  default_prompt: "Use the windows repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true